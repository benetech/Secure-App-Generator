Logging added AWS Cloud Logging For Eclipse
https://aws.amazon.com/eclipse/
(DOn't think it was needed.)

Based on ENVIRONMENT S3_DOWNLOAD_BUCKET 
If "QA" is in the bucket name then the server will be DEV 
otherwise it will be the Martus Production Server for both
Staging and Production environments on S3.
